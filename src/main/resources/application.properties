spring.application.name=auth-service
server.servlet.context-path=/auth
server.port=8084

# DATABASE
spring.datasource.url=${DATASOURCE_URL:jdbc:postgresql://localhost:5432/auth_database}
spring.datasource.username=${DATASOURCE_USERNAME:rota-facil}
spring.datasource.password=${DATASOURCE_PASSWORD:admin}


# JPA + HIBERNATE
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#  EUREKA
eureka.client.service-url.defaultZone=${EUREKA_URL:http://localhost:8081/eureka}
eureka.client.fetch-registry=true


# RABBIMQ
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USER:rota-facil}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:admin}

# auth exchange
rabbitmq.auth.exchange={RABBITMQ_AUTH_EXCHANGE:auth.events}
# routing keys
rabbitmq.file.user.created.routing.key=${RABBITMQ_USER_CREATED_ROUTING_KEY:user.created}
rabbitmq.file.user.updated.routing.key=${RABBITMQ_USER_UPDATED_ROUTING_KEY:user.updated}
rabbitmq.file.user.deleted.routing.key=${RABBITMQ_USER_DELETED_ROUTING_KEY:user.deleted}

rabbitmq.file.prefecture.created.routing.key=${RABBITMQ_PREFECTURE_CREATED_ROUTING_KEY:prefecture.created}
rabbitmq.file.prefecture.updated.routing.key=${RABBITMQ_PREFECTURE_UPDATED_ROUTING_KEY:prefecture.updated}
rabbitmq.file.prefecture.deleted.routing.key=${RABBITMQ_PREFECTURE_DELETED_ROUTING_KEY:prefecture.deleted}